datasource db {
  provider = "postgresql"
  // no url here in Prisma 7
}

generator client {
  provider = "prisma-client-js"
}

model User {
  id       Int    @id @default(autoincrement())
  email    String @unique
  password String
}
